# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release-production
on:
  workflow_dispatch:
    inputs:
      version:
        description: Package version
        required: true
jobs:
  deploy:
    name: Release stage production to AWS
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@master
        with:
          role-to-assume: arn:aws:iam::730335247138:role/GithubAction
          role-session-name: GitHubAction
          aws-region: eu-west-1
      - run: npx projen install:ci
      - run: yarn add @buildinginthecloud/buildinginthecloud.com@${{github.event.inputs.version}}
      - run: mv ./node_modules/@buildinginthecloud/buildinginthecloud.com cdk.out
      - run: npx projen deploy:production
      - uses: actions/upload-artifact@v3
        with:
          name: cdk-outputs-production
          path: cdk-outputs-production.json
